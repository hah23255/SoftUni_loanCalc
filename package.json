{
  "name": "loan-calculator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js",
    "test": "node tests/run-tests.js",
    "prepare-deploy": "node scripts/prepare-deploy.js",
    "deploy": "npm run prepare-deploy && firebase deploy",
    "deploy:hosting": "npm run prepare-deploy && firebase deploy --only hosting"
  }
}
